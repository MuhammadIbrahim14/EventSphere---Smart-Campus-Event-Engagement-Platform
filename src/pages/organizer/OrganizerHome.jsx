/**
 * Placeholder only — teammate replaces this with the real organizer dashboard.
 * Wired at `/organizer` behind OrganizerRoute (role must be `organizer`).
 */
export default function OrganizerHome() {
  return (
    <div className="page">
      <header className="page-header">
        <h1>Organizer</h1>
        <p className="muted">
          Panel mount ready. After pull, connect your organizer UI here (
          <code>src/pages/organizer/</code> + <code>OrganizerShell</code>).
        </p>
      </header>
    </div>
  )
}
